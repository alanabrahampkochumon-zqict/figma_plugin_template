# Figma Plugin Template: Vite + TS

## Setup

1. Configure plugin name and id in the `manifest.json` file.
2. Run the watcher using:

```
npm run watch
```

### Note

Keep all the logic that use `figma.` in the `main.ts` file or imported modules.
